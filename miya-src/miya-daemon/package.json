{
  "name": "miya-daemon",
  "private": true,
  "type": "module",
  "description": "Miya daemon runtime package",
  "scripts": {
    "start": "bun ../src/daemon/host.ts",
    "typecheck": "tsc -p ../tsconfig.json --noEmit"
  }
}

