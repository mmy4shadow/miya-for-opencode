{
  "name": "miya-daemon",
  "private": true,
  "type": "module",
  "description": "Miya daemon runtime package",
  "scripts": {
    "start": "tsx ../src/daemon/host.ts",
    "typecheck": "tsc -p ../tsconfig.json --noEmit"
  }
}

