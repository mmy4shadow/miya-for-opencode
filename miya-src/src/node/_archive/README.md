# node archive

This directory is reserved for compatibility-preserving moves when duplicate
node logic is consolidated between `node/` and `nodes/`.

Current pass keeps runtime behavior unchanged and does not move executable code
into `_archive` yet.
